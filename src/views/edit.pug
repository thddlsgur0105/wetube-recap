extends base.pug

block content 
    h4 Change Title of video #{video.id}
    form(method="POST")
        input(name="title", type="text", placeholder="Video Title", value=video.title, required)
        input(type="submit", value="Save") 